import { environment } from './environment';

export const constant = {
  oauthUrl: `${environment.oauthUrl}/oauth`,
  appUrl: `${environment.appUrl}/app`,
  adminUrl: `${environment.adminUrl}/admin`,
  comlainUrl: `${environment.userUrl}/complain-user`,
  complainOnlineUrl: `${environment.apiUrl}/complain-online`

};
